# Stream-Farm-Pro
StreamFarm Pro is a specialized Windows desktop utility designed to bypass background browser throttling, enabling the simultaneous viewing of multiple Live streams (2, 4, 6, or 8) without performance degradation. It leverages a custom Electron helper to ensure all active streams remain prioritized by the OS, regardless of window focus.
